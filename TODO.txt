TODO Demo version 1:
- drift setup
- Demo data
- Demo mode (ny bruger auto med i Andeby Badmintonklub, ved ny klub oprettelse ansøger Lin Bam and Lee Gong Vej automatisk)

TODO Demo version 2:
- Demo versions nummer 
- wrap API in oauth auth (se svar 2 her http://stackoverflow.com/questions/5406859/facebook-access-token-server-side-validation-for-iphone-app og https://developers.facebook.com/docs/facebook-login/manually-build-a-login-flow#checktoken)
- gatekeeper access token

TODO Demo version 3:
- center navbar text
- admin klub medlem: gør til træner, gør til admin. se http://jsfiddle.net/bmleite/PQvQ2/
- lav rediger klub side
- bruger opret og opdater: valider email, valider tlf
- sanitise input strenge fra POST og PUT (specielt "name")
- klub auto accepter alle nye spillere indstilling

TODO Demo version 4:
- testcases: getuser bad id
- testcases: træningspas
- testcases: alle brug af input->datetimes, tjek for input er string og længde. også dateutil.parse
- testcases: alle isinstance(jsoninput, int) but være try/catch valueerror med int() + test 
- testcases: GET bruger/id/træningspas incl bad id
- testcases: alle parsing af int i urls/params til ints (alle steder hvor int(bla) er kaldt (int(bla) kaste ValueException, og det er kodet som om det bare retunere false))
- testcases: GET klub træningspas
- testcases: brugere PUT

- custom errors i API?
- bedre errors in controllers

- konsistenstjek datatyper docs in bAdmin_main.py
- log warnings alle de steder i API hvor GET returnere første element af en liste, og der -mod forventning- er mere end ét element i listen

- refaktor bAdminApp.js til en controller pr fil
- refaktor .tilmeld-traening-btn css om til global liste-knap css

TODO Demo version 5:
- handle user create error i indexcontroller

===========
Før deploy:
===========
- ikke debug mode Flash
- ikke debug logging Angular
- brug angular minified versioner

===============
Kendte fejl:
===============
- Hvis en bruger slettes imens en klient har hentet fx en klub med liste af spiller hvor den slettede spiller er i, kan denne klient ikke POST/PUT da alle spillere nu ikke eksisterer. Klienten har ingen nem måde at tjekke på
- Hvis klient A og B har GETtet samme version af et obj, fx. klub, og klient A PUTer en opdatering til objektet, og klient B senere PUTer en opdatering til samme object, uden at have GETtet en opdatering, så mistes opdateringen fra A. Det gælder p.t. for acceptMemebershipRequest, rejectMemebershipRequest, confirmPractice, rejectPractice
- bAdminAPIService.acceptMemebershipRequest laver to requests. Hvis ét af de to fejler (og det anden ikke gør), er DB'en ikke længere konsistent mellem klub.membershipRequests og bruger.clubs (se implementering af bAdminAPIFactory.removeMemberFromClub for løsning $q.all(..))

===============
Feature ønsker:
===============
- Træningstrupper: opret/rediger, placer medlem på træningstrup, inviter træningstrup (mange medlemmer) ved opret træning
- Bruger rediger side / slet Bruger (?)
- Slet Klub (?) 
- Database / ikke in-memory DB
- HTTPS
- Bruger rettigheder: skal bruger X kunne kalder /practices eller /users og få listet alle træningsplan/brugere?
- GMail login
- Klub administrator kan redigere eksisterende træning (omend blot inviterede)
- Dokumenter dev miljø + deploy
- Gemmel IE browser blokering
- Backend: Bruger obj.clubs må ikke kunne opdateres til at fjerne en klub, hvor brugeren er admin eller coach

==================================
JS controller refaktor konsistens:
==================================
- vars
- lokale helpers
- lokale fns
- init
- scope fns
- scope helpers